databases:
  - name: catan-db
    plan: free

services:
  - type: web
    name: catan
    runtime: node
    plan: free
    buildCommand: npm install --include=dev && npm run build
    startCommand: npm start
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: catan-db
          property: connectionString
